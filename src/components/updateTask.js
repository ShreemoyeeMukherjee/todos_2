function onChange(e,taskData,settaskData)
{
    console.log(typeof(settaskData));
    settaskData((prevState)=>({
        ...prevState,
         [e.target.name]:e.target.value,

      }))
      

}
function onSubmit(e,task,taskData,settaskData,taskArray,settaskArray)
    {
        e.preventDefault();
        console.log("after task updation",taskData);
    
        settaskArray((prevState)=>{
           return  prevState.map((task)=>{
               return (task.id === taskData.id) ? {...task,...taskData}:{task}
            })
        })
        let i = 0;
        console.log("Task updation");
        for(i = 0;i<taskArray.length;i++)
        {
                  console.log(taskArray[i])
        }
        
    }
    


export {onChange,onSubmit};































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































 